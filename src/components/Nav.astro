---
import { site } from '../data/site';
import { withBase } from '../lib/path';

const cvHref = withBase(site.cvPath);

const links = [
  { href: '#a-propos', label: 'À propos' },
  { href: '#competences', label: 'Compétences' },
  { href: '#projets', label: 'Projets' },
  { href: '#reseau', label: 'Démo réseau' },
  { href: '#experience', label: 'Expérience' },
  { href: '#formation', label: 'Formation' },
  { href: '#contact', label: 'Contact' },
];
---

<header
  class="sticky top-0 z-50 border-b border-slate-200/70 bg-white/80 backdrop-blur-md dark:border-slate-800/70 dark:bg-slate-950/80"
>
  <nav
    class="mx-auto flex h-16 max-w-5xl items-center justify-between px-4 sm:px-6"
    aria-label="Navigation principale"
  >
    <a
      href="#hero"
      class="text-lg font-bold tracking-tight text-slate-900 dark:text-white"
    >
      YB<span class="text-accent-500">.</span>
    </a>

    <!-- Liens (desktop) -->
    <ul class="hidden items-center gap-1 md:flex">
      {
        links.map((link) => (
          <li>
            <a
              href={link.href}
              class="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition-colors hover:text-accent-600 dark:text-slate-300 dark:hover:text-accent-400"
            >
              {link.label}
            </a>
          </li>
        ))
      }
    </ul>

    <div class="flex items-center gap-2">
      <a
        href={cvHref}
        class="hidden rounded-md bg-accent-500 px-3 py-2 text-sm font-semibold text-white transition-colors hover:bg-accent-600 sm:inline-block"
      >
        CV
      </a>

      <!-- Bouton menu (mobile) -->
      <button
        id="menu-toggle"
        type="button"
        class="inline-flex h-9 w-9 items-center justify-center rounded-md text-slate-600 transition-colors hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800 md:hidden"
        aria-label="Ouvrir le menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg
          class="h-6 w-6"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.8"
          stroke="currentColor"
          aria-hidden="true"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Menu mobile -->
  <div
    id="mobile-menu"
    class="hidden border-t border-slate-200/70 bg-white px-4 py-3 dark:border-slate-800/70 dark:bg-slate-950 md:hidden"
  >
    <ul class="flex flex-col gap-1">
      {
        links.map((link) => (
          <li>
            <a
              href={link.href}
              class="block rounded-md px-3 py-2 text-base font-medium text-slate-700 hover:bg-slate-100 hover:text-accent-600 dark:text-slate-200 dark:hover:bg-slate-800 dark:hover:text-accent-400"
              data-mobile-link
            >
              {link.label}
            </a>
          </li>
        ))
      }
      <li>
        <a
          href={cvHref}
          class="block rounded-md bg-accent-500 px-3 py-2 text-base font-semibold text-white hover:bg-accent-600"
          data-mobile-link
        >
          Télécharger le CV
        </a>
      </li>
    </ul>
  </div>
</header>

<script is:inline>
  (() => {
    // Menu mobile : ouverture/fermeture.
    const menuBtn = document.getElementById('menu-toggle');
    const menu = document.getElementById('mobile-menu');
    menuBtn?.addEventListener('click', () => {
      const open = menu?.classList.toggle('hidden') === false;
      menuBtn.setAttribute('aria-expanded', String(open));
    });
    // Referme le menu après un clic sur un lien.
    document.querySelectorAll('[data-mobile-link]').forEach((el) => {
      el.addEventListener('click', () => {
        menu?.classList.add('hidden');
        menuBtn?.setAttribute('aria-expanded', 'false');
      });
    });
  })();
</script>
